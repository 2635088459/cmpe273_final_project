# EraseGraph — 3.0 Task Extension Testing Checklist

**Purpose:** Each team member must run the tests listed under their section to confirm that their new extension features work correctly and do not break the overall system.

---

## Vritika Malhotra — Reliability & Circuit Breaker

Features implemented: Retry/DLQ, Idempotency, Failure Injection, Circuit Breaker, API Gateway

### Unit Tests to Write / Run

- [ ] `CircuitBreakerService` — test that after 3 consecutive failures, state transitions from `CLOSED` → `OPEN`
- [ ] `CircuitBreakerService` — test that `canProcess()` returns `false` when state is `OPEN`
- [ ] `CircuitBreakerService` — test that state transitions `OPEN` → `HALF_OPEN` after 30 seconds
- [ ] `DlqReplayService` — test that `POST /admin/dlq/:queue/replay` re-publishes messages back to the main exchange
- [ ] `IdempotencyService` — test that processing the same `event_id` twice does not double-process the deletion step
- [ ] `IdempotencyService` — test that a `DUPLICATE_EVENT_IGNORED` proof event is recorded on second attempt

### Integration / Script Tests to Run

- [ ] Start the full stack with `docker-compose up`
- [ ] Submit a deletion request where `subject_id` starts with `fail-` to trigger the failure injection flag
- [ ] Verify in RabbitMQ Management UI (`http://localhost:15672`) that the message retries 3 times then lands in the DLQ
- [ ] Call `GET /admin/circuits` and confirm the failing service shows state `OPEN`
- [ ] Wait 30 seconds and call `GET /admin/circuits` again — state should transition to `HALF_OPEN`
- [ ] Manually publish the same RabbitMQ event twice and confirm the deletion step is only processed once
- [ ] Confirm the API gateway on port 3000 rejects requests missing the `X-Service-Token` header

---

## Member 2 (Asim Mohammed) — New Services + Proof Hash Chain

Features implemented: Search Cleanup Service, Analytics Cleanup Service (with delay), Notification Service, Proof Hash Chain

### Unit Tests to Write / Run

- [ ] `SearchConsumerService` — test that consuming a `DeletionRequested` event deletes the matching row from `search_index_documents`
- [ ] `SearchConsumerService` — test that a `DeletionStepSucceeded` event is published after successful deletion
- [ ] `AnalyticsConsumerService` — test that rows are soft-deleted (marked as deleted, not hard-deleted) in `analytics_events`
- [ ] `AnalyticsConsumerService` — test that the service waits the expected delay before publishing success
- [ ] `NotificationConsumerService` — test that a `DELETION_COMPLETE` row is inserted into `deletion_notifications` when deletion succeeds
- [ ] `NotificationConsumerService` — test that a `DELETION_FAILED` row is inserted when deletion fails
- [ ] `ProofHashUtil` — test that changing any field in an event produces a different hash
- [ ] `ProofHashUtil` — test that the hash chain is broken when a previous event is modified

### Integration / Script Tests to Run

- [ ] Start full stack with `docker-compose up` — confirm `analytics-cleanup-service`, `search-cleanup-service`, and `notification-service` containers start without errors
- [ ] Submit a deletion request and verify that `search_cleanup` and `analytics_cleanup` appear as steps in `GET /deletions/:id`
- [ ] Confirm `analytics_cleanup` step takes noticeably longer (5–10 seconds) while other steps finish first — demonstrating eventual consistency
- [ ] Call `GET /deletions/:id/notification` after completion and verify a `DELETION_COMPLETE` record is returned
- [ ] Call `GET /deletions/:id/proof/verify` and confirm response is `{ "verified": true }`
- [ ] Manually update one row in the `proof_events` table in PostgreSQL and call `/proof/verify` again — confirm response is `{ "verified": false }`

---

## Haoyuan Shan — Kubernetes + Observability

Features implemented: K8s Manifests, Pod Recovery, Metrics Endpoint, Rate Limiting, Health Aggregation

### Unit Tests to Run

Run all 6 spec files with:

```bash
cd backend
npx jest --testPathPattern="haoyuan_shan" --no-coverage --forceExit
```

Expected result: **28 tests passing** across:
- `haoyuan_shan_metrics.service.spec.ts`
- `haoyuan_shan_prometheus.service.spec.ts`
- `haoyuan_shan_metrics.controller.spec.ts`
- `haoyuan_shan_health-aggregator.service.spec.ts`
- `haoyuan_shan_health.controller.spec.ts`
- `haoyuan_shan_throttler-rate-limit.spec.ts`

### K8s Integration Script to Run

```bash
./scripts/haoyuan_shan_k8s_test.sh
```

Expected result: **34 tests passing** across:
- All K8s manifests parse (dry-run)
- All pods Running in namespace `erasegraph`
- Backend has 2 replicas
- `GET /metrics` returns JSON with correct keys
- `GET /metrics/prometheus` returns Prometheus text format
- `GET /health/all` returns `overall: UP` with all 4 downstream services
- Rate limiter returns 429 after burst of requests
- Killed pod recovers automatically and backend remains reachable

### Additional Manual Checks

- [ ] Confirm `helm lint helm/erasegraph/` passes with 0 errors
- [ ] Confirm `SIMULATE_FAILURE` is `"false"` in both `k8s/configmap.yaml` and `helm/erasegraph/values.yaml`
- [ ] Confirm no `erasegraph_secret` placeholder passwords remain in `k8s/secrets.yaml`

---

## Sakshat Patil — Frontend + Real-Time Features

Features implemented: Dashboard auto-refresh, Proof Timeline, SSE real-time updates, History page, Admin panel

### Unit Tests to Write / Run

- [ ] `DeletionRequestController` (SSE endpoint) — test that `GET /deletions/:id/stream` sets `Content-Type: text/event-stream` header
- [ ] `DeletionRequestController` (SSE endpoint) — test that the connection closes automatically when status becomes `COMPLETED` or `FAILED`
- [ ] History page component — test that the status filter dropdown correctly filters the displayed requests
- [ ] History page component — test that the subject ID search box filters requests by subject ID
- [ ] Admin page component — test that service health badges display green for `UP` and red for `DOWN`
- [ ] Admin page component — test that circuit breaker states (`CLOSED` / `OPEN` / `HALF_OPEN`) are displayed per service

### Manual / Browser Tests to Run

- [ ] Open `http://localhost:3000` and submit a deletion request — confirm the status updates automatically every 5 seconds without manual refresh
- [ ] Confirm the auto-refresh stops when status reaches `COMPLETED` or `FAILED`
- [ ] Confirm the "Stop auto-refresh" toggle button works
- [ ] Open `http://localhost:3000/history` — confirm all past deletion requests are listed with correct status badges
- [ ] Use the status filter dropdown to filter by `COMPLETED` — only completed requests should appear
- [ ] Use the search box to search by a known `subject_id` — only matching requests should appear
- [ ] Click the "View" button on a history row — confirm it navigates to the detail view for that request
- [ ] Open `http://localhost:3000/admin` — confirm all services show a green `UP` badge
- [ ] On the Admin page, confirm circuit breaker states are displayed (all should be `CLOSED` in a healthy system)
- [ ] Open the proof view for a completed deletion — confirm the `Proof Verified ✓` badge is shown
- [ ] Check that the SSE stream (`GET /deletions/:id/stream`) updates the dashboard in real time without polling delay

---

## Full-Stack Smoke Test (All Members)

After each member finishes their individual tests, run this final check together:

- [ ] `docker-compose up` — all services start without errors, no crash loops
- [ ] Submit a deletion for a `subject_id` that has data in primary DB, search index, and analytics — all 5+ steps complete
- [ ] The deletion request eventually reaches `COMPLETED` status
- [ ] `GET /deletions/:id/proof/verify` returns `{ "verified": true }`
- [ ] `GET /deletions/:id/notification` returns the GDPR confirmation record
- [ ] `GET /health/all` reports all services as `UP`
- [ ] `GET /metrics` shows updated counts reflecting the new deletion
- [ ] Jaeger UI (`http://localhost:16686`) shows distributed traces for the full deletion flow
